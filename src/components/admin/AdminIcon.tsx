/** Small mark in the admin sidebar. */
export function AdminIcon() {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img src="/brand/logo.png" alt="هات پست" style={{ height: 28, width: "auto" }} />
  );
}
